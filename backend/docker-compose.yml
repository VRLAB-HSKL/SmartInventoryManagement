version: '3.3'
services:
    database.db:
        image: postgres:14.2
        restart: always
        environment:
          - POSTGRES_USER=postgres_user
          - POSTGRES_PASSWORD=postgres_password
        ports:
          - '5433:5432'
        volumes: 
          - ./postgres-data:/var/lib/postgresql/data